export default interface FilesType {
  file_name: string;
  file_bytes: string;
  data: {
    id: string;
    size: string;
  };
  file_data: string;
  id: string;
}

export default interface FilesRecord {}
